

void bfs{
    temp = root;
    insert temp into queue;
    while(quee not empty){
        remove from queue into tmep;
        visit temp;
        if(temp.left != NULL){
            insert temp.eft into queue.
        }
        if(temp.right != NULL){
            insert temp.right into queue.
        }
    }
}